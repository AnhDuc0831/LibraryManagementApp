<Window x:Class="LibraryManageApp.BookInstanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManageApp"
        mc:Ignorable="d"
        Title="Book Instance Management" Height="550" Width="900"
        Background="#F0F4F8"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Style cho Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4299E1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3182CE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2C5282"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E53E3E"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C53030"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#38B2AC"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#319795"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho DataGrid -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#B3D4E5"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AlternatingRowBackground" Value="#EBF8FF"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>

        <Style x:Key="DataGridColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2C5282"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,14"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#4299E1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BEE3F8"/>
                    <Setter Property="Foreground" Value="#1A365D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E6F7FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho Label -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#2C5282" 
                CornerRadius="10,10,0,0" 
                Padding="25,18" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="📖 Book Instance Manager" 
                              FontSize="26" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="Manage individual book copies and their conditions" 
                              FontSize="13" 
                              Foreground="#B3D4E5" 
                              Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Total Instances" 
                              FontSize="11" 
                              Foreground="#B3D4E5" 
                              HorizontalAlignment="Center"/>
                    <Border Background="#4299E1" 
                            CornerRadius="20" 
                            Padding="15,8" 
                            Margin="0,5,0,0">
                        <TextBlock x:Name="txtInstanceCount"
                                  Text="{Binding ElementName=dgInstances, Path=Items.Count}" 
                                  FontSize="20" 
                                  FontWeight="Bold" 
                                  Foreground="White" 
                                  HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid Section -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="10" 
                Padding="20"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#90CDF4" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Background="#EBF8FF" 
                        CornerRadius="8" 
                        Padding="15,12" 
                        Margin="0,0,0,15">
                    <Grid>
                        <TextBlock Text="Instance List" 
                                  Style="{StaticResource FieldLabel}"
                                  FontSize="16"
                                  VerticalAlignment="Center"/>
                        <TextBlock Text="💡 Double-click on cells to edit condition" 
                                  FontSize="12" 
                                  Foreground="#4299E1"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  FontStyle="Italic"/>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="1"
                          x:Name="dgInstances" 
                          AutoGenerateColumns="False"
                          Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource DataGridColumnHeader}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Instance ID" 
                                           Binding="{Binding InstanceId}" 
                                           IsReadOnly="True" 
                                           Width="150"/>
                        <DataGridTextColumn Header="Condition" 
                                           Binding="{Binding Condition}" 
                                           Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="10" 
                Padding="20,15">
            <Border.Effect>
                <DropShadowEffect Color="#90CDF4" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#EBF8FF" 
                            CornerRadius="6" 
                            Padding="12,8" 
                            Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" 
                                      FontSize="16" 
                                      Margin="0,0,8,0" 
                                      VerticalAlignment="Center"/>
                            <TextBlock Text="Select an instance to remove or add new instances" 
                                      FontSize="13" 
                                      Foreground="#2C5282"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Right">
                    <Button Content="➕ Add Instance" 
                           Style="{StaticResource PrimaryButton}"
                           Click="Add_Click"
                           Margin="0,0,12,0"/>
                    <Button Content="🗑️ Remove" 
                           Style="{StaticResource DeleteButton}"
                           Click="Remove_Click"
                           Margin="0,0,12,0"/>
                    <Button Content="💾 Save Changes" 
                           Style="{StaticResource SuccessButton}"
                           Click="Save_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>